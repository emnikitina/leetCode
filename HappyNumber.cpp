#include <iostream>
#include <string>

using namespace std;

bool isHappy(int n) {
    int capacity[10];
    
};

int main(int argc, char** argv) {
    if (argc == 2)
        cout << "num is happy: " << isHappy(atoi(argv[1])) << endl;
    return 0;
}